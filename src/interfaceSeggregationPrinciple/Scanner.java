package interfaceSeggregationPrinciple;

public interface Scanner {
	/*
	 * Follows Interface seggregation principle does only scanning.
	 * Not overloaded with any other function
	 */
	void scan(Document d);
}
